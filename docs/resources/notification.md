---
page_title: "openstatus_notification Resource - terraform-provider-openstatus"
subcategory: ""
description: |-
  Manages an OpenStatus notification channel.
---

# openstatus_notification (Resource)

Manages an OpenStatus notification channel. Notifications are sent when a linked monitor detects a status change. Supports 12 provider types, each with its own configuration block.

## Example Usage

```terraform
resource "openstatus_notification" "slack" {
  name          = "Slack Alerts"
  provider_type = "slack"
  monitor_ids   = [openstatus_http_monitor.api.id]

  slack {
    webhook_url = "https://hooks.slack.com/services/xxx/yyy/zzz"
  }
}

resource "openstatus_notification" "email" {
  name          = "Email Alerts"
  provider_type = "email"

  email {
    email = "oncall@example.com"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `provider_type` (String) Notification provider type. Valid values: `discord`, `email`, `slack`, `pagerduty`, `opsgenie`, `webhook`, `telegram`, `sms`, `whatsapp`, `google_chat`, `grafana_oncall`, `ntfy`.

### Optional

- `name` (String) Display name of the notification channel.
- `monitor_ids` (Set of String) Set of monitor IDs to associate with this notification.
- `discord` (Block List, Max: 1) Discord configuration. See [below for nested schema](#nestedblock--discord).
- `email` (Block List, Max: 1) Email configuration. See [below for nested schema](#nestedblock--email).
- `slack` (Block List, Max: 1) Slack configuration. See [below for nested schema](#nestedblock--slack).
- `pagerduty` (Block List, Max: 1) PagerDuty configuration. See [below for nested schema](#nestedblock--pagerduty).
- `opsgenie` (Block List, Max: 1) Opsgenie configuration. See [below for nested schema](#nestedblock--opsgenie).
- `webhook` (Block List, Max: 1) Webhook configuration. See [below for nested schema](#nestedblock--webhook).
- `telegram` (Block List, Max: 1) Telegram configuration. See [below for nested schema](#nestedblock--telegram).
- `sms` (Block List, Max: 1) SMS configuration. See [below for nested schema](#nestedblock--sms).
- `whatsapp` (Block List, Max: 1) WhatsApp configuration. See [below for nested schema](#nestedblock--whatsapp).
- `google_chat` (Block List, Max: 1) Google Chat configuration. See [below for nested schema](#nestedblock--google_chat).
- `grafana_oncall` (Block List, Max: 1) Grafana OnCall configuration. See [below for nested schema](#nestedblock--grafana_oncall).
- `ntfy` (Block List, Max: 1) ntfy configuration. See [below for nested schema](#nestedblock--ntfy).

### Read-Only

- `id` (String) The ID of the notification.
- `created_at` (String) Timestamp when the notification was created.
- `updated_at` (String) Timestamp when the notification was last updated.

<a id="nestedblock--discord"></a>
### Nested Schema for `discord`

Required:

- `webhook_url` (String) Discord webhook URL.

<a id="nestedblock--email"></a>
### Nested Schema for `email`

Required:

- `email` (String) Email address to send notifications to.

<a id="nestedblock--slack"></a>
### Nested Schema for `slack`

Required:

- `webhook_url` (String) Slack incoming webhook URL.

<a id="nestedblock--pagerduty"></a>
### Nested Schema for `pagerduty`

Required:

- `integration_key` (String) PagerDuty integration key.

<a id="nestedblock--opsgenie"></a>
### Nested Schema for `opsgenie`

Required:

- `api_key` (String) Opsgenie API key.
- `region` (String) Opsgenie region. Valid values: `us`, `eu`.

<a id="nestedblock--webhook"></a>
### Nested Schema for `webhook`

Required:

- `endpoint` (String) Webhook endpoint URL.

Optional:

- `headers` (Block List) Custom headers to include. See [below for nested schema](#nestedblock--webhook--headers).

<a id="nestedblock--webhook--headers"></a>
### Nested Schema for `webhook.headers`

Required:

- `key` (String) Header name.
- `value` (String) Header value.

<a id="nestedblock--telegram"></a>
### Nested Schema for `telegram`

Required:

- `chat_id` (String) Telegram chat ID.

<a id="nestedblock--sms"></a>
### Nested Schema for `sms`

Required:

- `phone_number` (String) Phone number for SMS notifications.

<a id="nestedblock--whatsapp"></a>
### Nested Schema for `whatsapp`

Required:

- `phone_number` (String) Phone number for WhatsApp notifications.

<a id="nestedblock--google_chat"></a>
### Nested Schema for `google_chat`

Required:

- `webhook_url` (String) Google Chat webhook URL.

<a id="nestedblock--grafana_oncall"></a>
### Nested Schema for `grafana_oncall`

Required:

- `webhook_url` (String) Grafana OnCall webhook URL.

<a id="nestedblock--ntfy"></a>
### Nested Schema for `ntfy`

Required:

- `topic` (String) ntfy topic name.

Optional:

- `server_url` (String) Custom ntfy server URL. Defaults to the public ntfy server.
- `token` (String) ntfy authentication token.

## Import

Import using the notification ID:

```shell
terraform import openstatus_notification.example <notification_id>
```
